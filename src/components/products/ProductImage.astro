---
import { getImagePath } from '@/utils';

type Props = {
  src: string;
  alt: string;
  className: string;
};

const { src, alt, className } = Astro.props;

const currentImage = getImagePath(src);
---

<img src={currentImage} alt={alt} class:list={[className]} />
